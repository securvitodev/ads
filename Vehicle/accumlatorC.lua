local accumlatorSubText = '%'

local vehicle = getElementsByType ('vehicle')

local accumlatorState = getElementData(vehicle, 'veh.accumlatorState')

local defaultAccuState = setElementData(vehicle, 'veh.accumlatorState', 100)

addEventHandler('onClientRender', root, function ()
	if engineState = false  and 
		if lampaState = true then
			setTimer( function()

			) 
			accumlatorState = setElementData('veh.accumlatorState', accumlatorState - 0.00002)


end)